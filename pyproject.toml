[tool.poetry]
name = "electrolyte-fm"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
lightning = "^2.1.3"
jsonargparse = {extras = ["signatures"], version = "^4.27.3"}
torch = ">=2.0,<3.0"
torchvision = "^0.16.2"
transformers = ">=4.0,<5.0"
deepspeed = ">=0.9,<1.0"
wandb = "^0.16.2"
typer = "^0.9.0"
h5py = "^3.10.0"
mpi4py = "^3.1.5"


[tool.poetry.group.dev.dependencies]
pytest = "^8.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.pytest.ini_options]
testpaths = ["test"]
addopts = "--color yes"
